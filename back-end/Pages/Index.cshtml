@page
@model IndexModel
@{
    ViewData["Title"] = "ChatApp - Home";
}

<!-- HERO (full width) -->
<section class="bg-dark text-white text-center py-5 d-flex align-items-center justify-content-center" style="min-height: 60vh;">
    <div class="container-fluid">
        <h1 class="display-2 fw-bold mb-3">ChatApp 💬</h1>
        <p class="lead mb-4">
            Chat realtime sicura e moderna con <strong>ASP.NET Core</strong>, <strong>WebSocket</strong> e <strong>JWT</strong>.
        </p>
        <div class="d-flex flex-column flex-sm-row justify-content-center gap-3">
            <a href="#" class="btn btn-outline-light" data-bs-toggle="modal" data-bs-target="#loginModal">Accedi</a>
            <a href="#" class="btn btn-success" data-bs-toggle="modal" data-bs-target="#registerModal">Registrati</a>
        </div>
    </div>
</section>

<!-- FEATURES (lasciata centrata, con container classico) -->
<section class="py-5 bg-light text-center">
    <div class="container">
        <h2 class="mb-5 fw-bold">Perché scegliere ChatApp</h2>
        <div class="row g-4">
            <div class="col-md-4">
                <div class="card border-0 shadow-sm h-100">
                    <div class="card-body">
                        <h5 class="card-title fw-semibold">🔒 Sicurezza</h5>
                        <p>Hashing PBKDF2, salt e token JWT per protezione totale dei dati.</p>
                    </div>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card border-0 shadow-sm h-100">
                    <div class="card-body">
                        <h5 class="card-title fw-semibold">⚡ Realtime</h5>
                        <p>Messaggi istantanei tramite WebSocket, senza attese né polling.</p>
                    </div>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card border-0 shadow-sm h-100">
                    <div class="card-body">
                        <h5 class="card-title fw-semibold">🧱 Architettura pulita</h5>
                        <p>Servizi, DTO e mapper per un codice chiaro e facilmente scalabile.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
